import React from 'react';
import { Marker } from '@react-google-maps/api';
/*
  Title
  Creator name
  location as address
  date
  description
*/

function MarkerCreator({ title, creator, location, date, description }) {
  
}